//Namespace
if (!this.fe || typeof this.fe !== 'object') {
	this.fe = {};
} 

/* Map Model */
fe.BasemapModel = Backbone.Model.extend({
    initialize: function (args) {
		var id = args.id;
		var xxx = 0;
    
    }
});